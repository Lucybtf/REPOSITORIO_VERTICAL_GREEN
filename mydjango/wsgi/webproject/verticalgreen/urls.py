from django.conf.urls import url

from . import views

urlpatterns = [
   # url(r'^$', views.index, name='index'),
  # url(r'^crear/', 'verticalgreen.views.get_name', name='get_name'),
    #url(r'^post/form_upload.html$', 'verticalgreen.views.post_form_upload', name='post_form_upload'),
	#url(r'^contact/$', views.contact, name='contact'),
]